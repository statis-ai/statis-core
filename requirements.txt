# Worker: same deps as api/ so Nixpacks install works (api/ may not be present during install).
# Keep in sync with api/requirements.txt.
fastapi
uvicorn
sqlalchemy
alembic
psycopg[binary]
pydantic
pytest
httpx
testcontainers[postgres]
