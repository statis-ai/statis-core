export const SERIES = {
  approved:  "var(--accent)",       // rust — primary positive
  completed: "var(--seal)",         // deep green
  good:      "var(--good)",         // success state
  denied:    "var(--bad)",          // red
  escalated: "var(--warn)",         // amber
  muted:     "var(--ink-muted)",    // baseline
} as const;
